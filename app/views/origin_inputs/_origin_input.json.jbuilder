json.extract! origin_input, :id, :prev_hash, :output_index, :script, :output_value, :sequence, :addresses, :script_type, :created_at, :updated_at
json.url origin_input_url(origin_input, format: :json)